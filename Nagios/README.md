# Nagios

Notes on installing and using Nagios for infrastructure monitoring.

## Contents

- `nagios installation.txt` — Installing Nagios Core and plugins from source on a Linux server
  (dependencies, `nagios`/`nagcmd` users and groups, build/configure steps).
- `NagiosXI` — Notes on Nagios XI, the commercial/enterprise edition of Nagios.
